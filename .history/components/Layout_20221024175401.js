const Layout =  ({children}) => {
    // debugger
    // const clidren = param.children;
    console.log(children);
    return (
        <>
       
        <main>{children}</main>
        </>

    )
};

export default Layout;